#!/usr/bin/env gura
import(markdown)

text@en = R'''
# cbridge Module

The `cbridge` module ...

## Module Function

${markdown.makedoc@function(`en
)}

'''.embed()

markdown.moduledoc(locals(), %{`en => text@en})
