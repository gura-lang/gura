#!/usr/bin/env gura
import(moddoc)

text@en = R'''
# modbuild Module

The `modbuild` module ...

## Module Function

${moddoc.reference(`en
)}

'''.embed()

moddoc.register(locals(), %{`en => text@en})
