#!/usr/bin/env gura
import(markdown)

text@en = R'''
# modbuild Module

The `modbuild` module ...

## Module Function

${markdown.makedoc@function(`en
)}

'''.embed()

markdown.moduledoc(locals(), %{`en => text@en})
