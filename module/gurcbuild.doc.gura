#!/usr/bin/env gura
import(moddoc)

text@en = R'''
# gurcbuild Module

The `gurcbuild` module ...

## Module Function

${moddoc.reference(`en
)}

'''.embed()

moddoc.register(locals(), %{`en => text@en})
