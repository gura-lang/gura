#!/usr/bin/env gura
import(moddoc)

text@en = R'''
# tokenizer Module

The `tokenizer` module ...

## Module Function

${moddoc.reference(`en
)}

'''.embed()

moddoc.register(locals(), %{`en => text@en})
