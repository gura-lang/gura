#!/usr/bin/env gura
import(moddoc)
import(calendar)

text@en = R'''
# calendar Module

The `calendar` module ...

## Module Function

${moddoc.reference(`en
calendar.calendar
)}

'''.embed()

moddoc.register(locals(), %{`en => text@en})
