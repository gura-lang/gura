#!/usr/bin/env gura
import(plotter)

text@en = R'''
# plotter Module

The `plotter` module ...

## Module Function

${markdown.makedoc@function(`en
)}

'''.embed()

markdown.makedoc@module(locals(), %{`en => text@en})
