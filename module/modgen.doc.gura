#!/usr/bin/env gura
import(markdown)

text@en = R'''
# modgen Module

The `modgen` module ...

## Module Function

${markdown.makedoc@function(`en
)}

'''.embed()

markdown.makedoc@module(locals(), %{`en => text@en})
