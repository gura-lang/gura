path.walk('', 1, '*.cpp', '*.h') {|fileName|
	println(fileName)
	lines = open(fileName).readlines():list
	lines = lines::replace('ascript/', 'gura/')
	open(fileName, 'w').dosmode(false).print(lines)
	//print(lines)
}
