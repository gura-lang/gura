#!/usr/bin/env gura
import(markdown)

text@en = R'''
# emf Module

The `emf` module provices measures to read/write EMF (Enhanced Meta File) files.
'''

markdown.makedoc@module(locals(), %{`en => text@en})
