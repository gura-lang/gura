#!/usr/bin/env gura
import(markdown)

write(doc:markdown.document) = {
	doc << R'''
	# mysql Module
	## Abstract
	## Sample
	'''
	doc << R'''
	## Module Functions
	'''
	doc << [
	]
	doc
}

if (__name__ == '__main__') {
	write(markdown.document()).render@html(sys.stdout)
}
