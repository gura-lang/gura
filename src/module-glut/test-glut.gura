#!/usr/bin/env gura
import(helper.test) {*}
import(glut) {*}

testcase {
	display() = {
		println('display')
	}
	glutInit(sys.argv);
	glutCreateWindow("hello");
	glutDisplayFunc(display);
	glutMainLoop();
}
