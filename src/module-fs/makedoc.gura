#!/usr/bin/env gura
import(markdown)

makedoc(doc:markdown.document) = {
	doc << [
		fs.chdir
		fs.chmod
		fs.copy
		fs.cpdir
		fs.getcwd
		fs.mkdir
		fs.remove
		fs.rename
		fs.rmdir
	]
}

if (__name__ == '__main__') {
	markdown.document {|doc|
		makedoc(doc)
		doc.render@html(sys.stdout)
	}
}
