#!/usr/bin/env gura
import(markdown)
import(conio)

doc = markdown.document()

doc << [
	fs.chdir
	fs.chmod
	fs.copy
	fs.cpdir
	fs.getcwd
	fs.mkdir
	fs.remove
	fs.rename
	fs.rmdir
]

doc.render@html(sys.stdout)
