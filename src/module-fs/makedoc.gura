#!/usr/bin/env gura
import(markdown)

write(doc:markdown.document) = {
	doc << [
		fs.chdir
		fs.chmod
		fs.copy
		fs.cpdir
		fs.getcwd
		fs.mkdir
		fs.remove
		fs.rename
		fs.rmdir
	]
	doc
}

if (__name__ == '__main__') {
	write(markdown.document()).render@html(sys.stdout)
}
