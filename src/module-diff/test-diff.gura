#!/usr/bin/env gura
import(helper.test) {*}
import(diff)

testcase {
	diff.unidiff@stream('module-diff-a.cpp', 'module-diff.cpp', sys.stdout)
	//printf('%4d%4d %s%s\n', edits:*lineno@L, edits:*lineno@R, edits:*mark, edits:*text)
}
