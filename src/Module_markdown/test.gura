#!/usr/bin/env gura
import(markdown)

text = R'''
paragraph

<table>
</table>

paragraph
'''

markdown.document {|doc|
	doc << text
	doc.print()
	//doc.output_html()
	//doc.output_rtf()
	//doc.output_console()
	refs = [doc.refs]
	if (!refs.isempty()) {
		println('** reference list **')
		refs:*print()
	}
}
