#!/usr/bin/env gura
import(modbuild)
builder = modbuild.Builder()
if (sys.build == `msc) {
} elsif (sys.build == `gnuc) {
	builder.debFiles = ['libgl1-mesa-dev', 'libglu1-mesa-dev']
	builder.rpmFiles = ['mesa-libGL-devel', 'mesa-libGLU-devel']
}
builder.build('opengl', ['module-opengl.cpp'])
