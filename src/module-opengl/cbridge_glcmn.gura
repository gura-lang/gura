#!/usr/bin/env gura
import(cbridge)

cbridge.AddTypeConvs {
	'unsigned_char@:const' => TypeConv(
			'string', 'Once', [],
			'const unsigned char *${arg.name} = reinterpret_cast<const unsigned char *>(args.GetString(${idx}));',
			nil,
			R'''
			const char *_rtn = reinterpret_cast<const char *>(${fi.MakeCaller()});
			return ReturnValue(env, sig, args, Value(_rtn));
			''')
	'string[]' => TypeConv(
			'string', 'Once', ['List'],
			'const ValueList &${arg.name} = args.GetList(${idx});',
			nil,
			nil)
	'GLenum' => TypeConv(
			'number', 'Once', [],
			'GLenum ${arg.name} = static_cast<GLenum>(args.GetInt(${idx}));',
			nil,
			R'''
			GLenum _rtn = ${fi.MakeCaller()};
			return ReturnValue(env, sig, args, Value(_rtn));
			''')
	'GLboolean' => TypeConv(
			'boolean', 'Once', [],
			'GLboolean ${arg.name} = (args.GetBoolean(${idx})? GL_TRUE : GL_FALSE);',
			nil,
			R'''
			GLboolean _rtn = ${fi.MakeCaller()};
			return ReturnValue(env, sig, args, Value(_rtn != GL_FALSE));
			''')
	'GLbitfield' => TypeConv(
			'number', 'Once', [],
			'GLbitfield ${arg.name} = args.GetUInt(${idx});',
			nil,
			R'''
			GLbitfield _rtn = ${fi.MakeCaller()};
			return ReturnValue(env, sig, args, Value(_rtn));
			''')
	'GLbyte' => TypeConv(
			'number', 'Once', [],
			'GLbyte ${arg.name} = args.GetChar(${idx});',
			nil,
			R'''
			GLbyte _rtn = ${fi.MakeCaller()};
			return ReturnValue(env, sig, args, Value(_rtn));
			''')
	'GLshort' => TypeConv(
			'number', 'Once', [],
			'GLshort ${arg.name} = args.GetShort(${idx});',
			nil,
			R'''
			GLshort _rtn = ${fi.MakeCaller()};
			return ReturnValue(env, sig, args, Value(_rtn));
			''')
	'GLint' => TypeConv(
			'number', 'Once', [],
			'GLint ${arg.name} = args.GetInt(${idx});',
			nil,
			R'''
			GLint _rtn = ${fi.MakeCaller()};
			return ReturnValue(env, sig, args, Value(_rtn));
			''')
	'GLsizei' => TypeConv(
			'number', 'Once', [],
			'GLsizei ${arg.name} = args.GetInt(${idx});',
			nil,
			R'''
			GLsizei _rtn = ${fi.MakeCaller()};
			return ReturnValue(env, sig, args, Value(_rtn));
			''')
	'GLubyte' => TypeConv(
			'number', 'Once', [],
			'GLubyte ${arg.name} = args.GetUChar(${idx});',
			nil,
			R'''
			GLubyte _rtn = ${fi.MakeCaller()};
			return ReturnValue(env, sig, args, Value(_rtn));
			''')
	'GLushort' => TypeConv(
			'number', 'Once', [],
			'GLushort ${arg.name} = args.GetUShort(${idx});',
			nil,
			R'''
			GLushort _rtn = ${fi.MakeCaller()};
			return ReturnValue(env, sig, args, Value(_rtn));
			''')
	'GLuint' => TypeConv(
			'number', 'Once', [],
			'GLuint ${arg.name} = args.GetUInt(${idx});',
			nil,
			R'''
			GLuint _rtn = ${fi.MakeCaller()};
			return ReturnValue(env, sig, args, Value(_rtn));
			''')
	'GLfloat' => TypeConv(
			'number', 'Once', [],
			'GLfloat ${arg.name} = args.GetFloat(${idx});',
			nil,
			R'''
			GLfloat _rtn = ${fi.MakeCaller()};
			return ReturnValue(env, sig, args, Value(_rtn));
			''')
	'GLclampf' => TypeConv(
			'number', 'Once', [],
			'GLclampf ${arg.name} = args.GetFloat(${idx});',
			nil,
			R'''
			GLclampf _rtn = ${fi.MakeCaller()};
			return ReturnValue(env, sig, args, Value(_rtn));
			''')
	'GLdouble' => TypeConv(
			'number', 'Once', [],
			'GLdouble ${arg.name} = args.GetDouble(${idx});',
			nil,
			R'''
			GLdouble _rtn = ${fi.MakeCaller()};
			return ReturnValue(env, sig, args, Value(_rtn));
			''')
	'GLclampd' => TypeConv(
			'number', 'Once', [],
			'GLclampd ${arg.name} = args.GetDouble(${idx});',
			nil,
			R'''
			GLclampd _rtn = ${fi.MakeCaller()};
			return ReturnValue(env, sig, args, Value(_rtn));
			''')
	'GLenum@' => TypeConv(
			'number', 'Once', ['List'],
			R'''
			AutoPtr<Array<GLenum> > _${arg.name}(CreateArrayFromList<GLenum>(args.GetList(${idx})));
			GLenum *${arg.name} = _${arg.name}->GetPointer();
			''',
			nil,
			nil)
	'GLenum@:const' => TypeConv(
			'number', 'Once', ['List'],
			R'''
			AutoPtr<Array<GLenum> > _${arg.name}(CreateArrayFromList<GLenum>(args.GetList(${idx})));
			GLenum *${arg.name} = _${arg.name}->GetPointer();
			''',
			nil,
			nil)
	'GLboolean@' => TypeConv(
			'boolean', 'Once', ['List'],
			R'''
			AutoPtr<Array<GLboolean> > _${arg.name}(CreateArrayFromList<GLboolean>(args.GetList(${idx})));
			GLboolean *${arg.name} = _${arg.name}->GetPointer();
			''',
			nil,
			nil)
	'GLboolean@:const' => TypeConv(
			'boolean', 'Once', ['List'],
			R'''
			AutoPtr<Array<GLboolean> > _${arg.name}(CreateArrayFromList<GLboolean>(args.GetList(${idx})));
			GLboolean *${arg.name} = _${arg.name}->GetPointer();
			''',
			nil,
			nil)
	'GLboolean@1:const' => TypeConv(
			'boolean', 'Once', ['List'],
			R'''
			AutoPtr<Array<GLboolean> > _${arg.name}(CreateArrayFromList<GLboolean>(args.GetList(${idx})));
			GLboolean *${arg.name} = _${arg.name}->GetPointer();
			if (_${arg.name}->GetSize() != 1) {
				sig.SetError(ERR_ValueError, "the list must have one element");
				return Value::Null;
			}''',
			nil,
			nil)
	'GLboolean@2:const' => TypeConv(
			'boolean', 'Once', ['List'],
			R'''
			AutoPtr<Array<GLboolean> > _${arg.name}(CreateArrayFromList<GLboolean>(args.GetList(${idx})));
			GLboolean *${arg.name} = _${arg.name}->GetPointer();
			if (_${arg.name}->GetSize() != 2) {
				sig.SetError(ERR_ValueError, "the list must have two elements");
				return Value::Null;
			}''',
			nil,
			nil)
	'GLboolean@3:const' => TypeConv(
			'boolean', 'Once', ['List'],
			R'''
			AutoPtr<Array<GLboolean> > _${arg.name}(CreateArrayFromList<GLboolean>(args.GetList(${idx})));
			GLboolean *${arg.name} = _${arg.name}->GetPointer();
			if (_${arg.name}->GetSize() != 3) {
				sig.SetError(ERR_ValueError, "the list must have three elements");
				return Value::Null;
			}''',
			nil,
			nil)
	'GLbyte@:const' => TypeConv(
			'array_char', 'Once', ['NoMap'],
			R'''
			Array<char> *_${arg.name} = Object_array<char>::GetObject(args, ${idx})->GetArray();
			GLbyte *${arg.name} = reinterpret_cast<GLbyte *>(_${arg.name}->GetPointer());
			''',
			nil,
			nil)
	'GLbyte@3:const' => TypeConv(
			'array_char', 'Once', ['NoMap'],
			R'''
			Array<char> *_${arg.name} = Object_array<char>::GetObject(args, ${idx})->GetArray();
			GLbyte *${arg.name} = reinterpret_cast<GLbyte *>(_${arg.name}->GetPointer());
			if (_${arg.name}->GetSize() != 3) {
				sig.SetError(ERR_ValueError, "the list must have three elements");
				return Value::Null;
			}''',
			nil,
			nil)
	'GLbyte@4:const' => TypeConv(
			'array_char', 'Once', ['NoMap'],
			R'''
			Array<char> *_${arg.name} = Object_array<char>::GetObject(args, ${idx})->GetArray();
			GLbyte *${arg.name} = reinterpret_cast<GLbyte *>(_${arg.name}->GetPointer());
			if (_${arg.name}->GetSize() != 4) {
				sig.SetError(ERR_ValueError, "the list must have three elements");
				return Value::Null;
			}''',
			nil,
			nil)
	'GLbyte@pname:const' => TypeConv(
			'array_char', 'Once', ['NoMap'],
			R'''
			Array<char> *_${arg.name} = Object_array<char>::GetObject(args, ${idx})->GetArray();
			GLbyte *${arg.name} = reinterpret_cast<GLbyte *>(_${arg.name}->GetPointer());
			if (!CheckParamCount(pname, _${arg.name}->GetSize())) {
				sig.SetError(ERR_ValueError,
						"the list must have %d elements", GetParamCount(pname));
				return Value::Null;
			}''',
			nil,
			nil)
	'GLchar@' => TypeConv(
			'array_char', 'Once', ['NoMap'],
			R'''
			Array<char> *_${arg.name} = Object_array<char>::GetObject(args, ${idx})->GetArray();
			GLchar *${arg.name} = reinterpret_cast<GLchar *>(_${arg.name}->GetPointer());
			''',
			nil,
			nil)
	'GLchar@:const' => TypeConv(
			'array_char', 'Once', ['NoMap'],
			R'''
			Array<char> *_${arg.name} = Object_array<char>::GetObject(args, ${idx})->GetArray();
			GLchar *${arg.name} = reinterpret_cast<GLchar *>(_${arg.name}->GetPointer());
			''',
			nil,
			nil)
	'GLubyte@:const' => TypeConv(
			'array_uchar', 'Once', ['NoMap'],
			R'''
			Array<UChar> *_${arg.name} = Object_array<UChar>::GetObject(args, ${idx})->GetArray();
			GLubyte *${arg.name} = reinterpret_cast<GLubyte *>(_${arg.name}->GetPointer());
			''',
			nil,
			R'''
			const GLubyte *_rtn = ${fi.MakeCaller()};
			return ReturnValue(env, sig, args, Value(reinterpret_cast<const char *>(_rtn)));
			''')
	'GLubyte@' => TypeConv(
			'array_uchar', 'Once', ['NoMap'],
			R'''
			Array<UChar> *_${arg.name} = Object_array<UChar>::GetObject(args, ${idx})->GetArray();
			GLubyte *${arg.name} = reinterpret_cast<GLubyte *>(_${arg.name}->GetPointer());
			''',
			nil,
			nil)
	'GLubyte@3:const' => TypeConv(
			'array_uchar', 'Once', ['NoMap'],
			R'''
			Array<UChar> *_${arg.name} = Object_array<UChar>::GetObject(args, ${idx})->GetArray();
			GLubyte *${arg.name} = reinterpret_cast<GLubyte *>(_${arg.name}->GetPointer());
			if (_${arg.name}->GetSize() != 3) {
				sig.SetError(ERR_ValueError, "the list must have three elements");
				return Value::Null;
			}''',
			nil,
			nil)
	'GLubyte@4:const' => TypeConv(
			'array_uchar', 'Once', ['NoMap'],
			R'''
			Array<UChar> *_${arg.name} = Object_array<UChar>::GetObject(args, ${idx})->GetArray();
			GLubyte *${arg.name} = reinterpret_cast<GLubyte *>(_${arg.name}->GetPointer());
			if (_${arg.name}->GetSize() != 4) {
				sig.SetError(ERR_ValueError, "the list must have three elements");
				return Value::Null;
			}''',
			nil,
			nil)
	'GLubyte@pname:const' => TypeConv(
			'array_uchar', 'Once', ['NoMap'],
			R'''
			Array<UChar> *_${arg.name} = Object_array<UChar>::GetObject(args, ${idx})->GetArray();
			GLubyte *${arg.name} = reinterpret_cast<GLubyte *>(_${arg.name}->GetPointer());
			if (!CheckParamCount(pname, _${arg.name}->GetSize())) {
				sig.SetError(ERR_ValueError,
						"the list must have %d elements", GetParamCount(pname));
				return Value::Null;
			}''',
			nil,
			nil)
	'GLshort@:const' => TypeConv(
			'array_short', 'Once', ['NoMap'],
			R'''
			Array<short> *_${arg.name} = Object_array<short>::GetObject(args, ${idx})->GetArray();
			GLshort *${arg.name} = reinterpret_cast<GLshort *>(_${arg.name}->GetPointer());
			''',
			nil,
			nil)
	'GLshort@1:const' => TypeConv(
			'array_short', 'Once', ['NoMap'],
			R'''
			Array<short> *_${arg.name} = Object_array<short>::GetObject(args, ${idx})->GetArray();
			GLshort *${arg.name} = reinterpret_cast<GLshort *>(_${arg.name}->GetPointer());
			if (_${arg.name}->GetSize() != 1) {
				sig.SetError(ERR_ValueError, "the list must have one element");
				return Value::Null;
			}''',
			nil,
			nil)
	'GLshort@2:const' => TypeConv(
			'array_short', 'Once', ['NoMap'],
			R'''
			Array<short> *_${arg.name} = Object_array<short>::GetObject(args, ${idx})->GetArray();
			GLshort *${arg.name} = reinterpret_cast<GLshort *>(_${arg.name}->GetPointer());
			if (_${arg.name}->GetSize() != 2) {
				sig.SetError(ERR_ValueError, "the list must have two elements");
				return Value::Null;
			}''',
			nil,
			nil)
	'GLshort@3:const' => TypeConv(
			'array_short', 'Once', ['NoMap'],
			R'''
			Array<short> *_${arg.name} = Object_array<short>::GetObject(args, ${idx})->GetArray();
			GLshort *${arg.name} = reinterpret_cast<GLshort *>(_${arg.name}->GetPointer());
			if (_${arg.name}->GetSize() != 3) {
				sig.SetError(ERR_ValueError, "the list must have three elements");
				return Value::Null;
			}''',
			nil,
			nil)
	'GLshort@4:const' => TypeConv(
			'array_short', 'Once', ['NoMap'],
			R'''
			Array<short> *_${arg.name} = Object_array<short>::GetObject(args, ${idx})->GetArray();
			GLshort *${arg.name} = reinterpret_cast<GLshort *>(_${arg.name}->GetPointer());
			if (_${arg.name}->GetSize() != 4) {
				sig.SetError(ERR_ValueError, "the list must have three elements");
				return Value::Null;
			}''',
			nil,
			nil)
	'GLshort@pname:const' => TypeConv(
			'array_short', 'Once', ['NoMap'],
			R'''
			Array<short> *_${arg.name} = Object_array<short>::GetObject(args, ${idx})->GetArray();
			GLshort *${arg.name} = reinterpret_cast<GLshort *>(_${arg.name}->GetPointer());
			if (!CheckParamCount(pname, ${arg.name}.GetSize())) {
				sig.SetError(ERR_ValueError,
						"the list must have %d elements", GetParamCount(pname));
				return Value::Null;
			}''',
			nil,
			nil)
	'GLushort@' => TypeConv(
			'array_ushort', 'Once', ['NoMap'],
			R'''
			Array<UShort> *_${arg.name} = Object_array<UShort>::GetObject(args, ${idx})->GetArray();
			GLushort *${arg.name} = reinterpret_cast<GLushort *>(_${arg.name}->GetPointer());
			''',
			nil,
			nil)
	'GLushort@:const' => TypeConv(
			'array_ushort', 'Once', ['NoMap'],
			R'''
			Array<UShort> *_${arg.name} = Object_array<UShort>::GetObject(args, ${idx})->GetArray();
			GLushort *${arg.name} = reinterpret_cast<GLushort *>(_${arg.name}->GetPointer());
			''',
			nil,
			nil)
	'GLushort@3:const' => TypeConv(
			'array_ushort', 'Once', ['NoMap'],
			R'''
			Array<UShort> *_${arg.name} = Object_array<UShort>::GetObject(args, ${idx})->GetArray();
			GLushort *${arg.name} = reinterpret_cast<GLushort *>(_${arg.name}->GetPointer());
			if (_${arg.name}->GetSize() != 3) {
				sig.SetError(ERR_ValueError, "the list must have three elements");
				return Value::Null;
			}''',
			nil,
			nil)
	'GLushort@4:const' => TypeConv(
			'array_ushort', 'Once', ['NoMap'],
			R'''
			Array<UShort> *_${arg.name} = Object_array<UShort>::GetObject(args, ${idx})->GetArray();
			GLushort *${arg.name} = reinterpret_cast<GLushort *>(_${arg.name}->GetPointer());
			if (_${arg.name}->GetSize() != 4) {
				sig.SetError(ERR_ValueError, "the list must have three elements");
				return Value::Null;
			}''',
			nil,
			nil)
	'GLushort@pname:const' => TypeConv(
			'array_ushort', 'Once', ['NoMap'],
			R'''
			Array<UShort> *_${arg.name} = Object_array<UShort>::GetObject(args, ${idx})->GetArray();
			GLushort *${arg.name} = reinterpret_cast<GLushort *>(_${arg.name}->GetPointer());
			if (!CheckParamCount(pname, _${arg.name}->GetSize())) {
				sig.SetError(ERR_ValueError,
						"the list must have %d elements", GetParamCount(pname));
				return Value::Null;
			}''',
			nil,
			nil)
	'GLint@' => TypeConv(
			'array_int', 'Once', ['NoMap'],
			R'''
			Array<int> *_${arg.name} = Object_array<int>::GetObject(args, ${idx})->GetArray();
			GLint *${arg.name} = reinterpret_cast<GLint *>(_${arg.name}->GetPointer());
			''',
			nil,
			nil)
	'GLint@:const' => TypeConv(
			'array_int', 'Once', ['NoMap'],
			R'''
			Array<int> *_${arg.name} = Object_array<int>::GetObject(args, ${idx})->GetArray();
			GLint *${arg.name} = reinterpret_cast<GLint *>(_${arg.name}->GetPointer());
			''',
			nil,
			nil)
	'GLint@1:const' => TypeConv(
			'array_int', 'Once', ['NoMap'],
			R'''
			Array<int> *_${arg.name} = Object_array<int>::GetObject(args, ${idx})->GetArray();
			GLint *${arg.name} = reinterpret_cast<GLint *>(_${arg.name}->GetPointer());
			if (_${arg.name}->GetSize() != 1) {
				sig.SetError(ERR_ValueError, "the list must have one element");
				return Value::Null;
			}''',
			nil,
			nil)
	'GLint@2:const' => TypeConv(
			'array_int', 'Once', ['NoMap'],
			R'''
			Array<int> *_${arg.name} = Object_array<int>::GetObject(args, ${idx})->GetArray();
			GLint *${arg.name} = reinterpret_cast<GLint *>(_${arg.name}->GetPointer());
			if (_${arg.name}->GetSize() != 2) {
				sig.SetError(ERR_ValueError, "the list must have two elements");
				return Value::Null;
			}''',
			nil,
			nil)
	'GLint@3:const' => TypeConv(
			'array_int', 'Once', ['NoMap'],
			R'''
			Array<int> *_${arg.name} = Object_array<int>::GetObject(args, ${idx})->GetArray();
			GLint *${arg.name} = reinterpret_cast<GLint *>(_${arg.name}->GetPointer());
			if (_${arg.name}->GetSize() != 3) {
				sig.SetError(ERR_ValueError, "the list must have three elements");
				return Value::Null;
			}''',
			nil,
			nil)
	'GLint@4:const' => TypeConv(
			'array_int', 'Once', ['NoMap'],
			R'''
			Array<int> *_${arg.name} = Object_array<int>::GetObject(args, ${idx})->GetArray();
			GLint *${arg.name} = reinterpret_cast<GLint *>(_${arg.name}->GetPointer());
			if (_${arg.name}->GetSize() != 4) {
				sig.SetError(ERR_ValueError, "the list must have three elements");
				return Value::Null;
			}''',
			nil,
			nil)
	'GLint@pname:const' => TypeConv(
			'array_int', 'Once', ['NoMap'],
			R'''
			Array<int> *_${arg.name} = Object_array<int>::GetObject(args, ${idx})->GetArray();
			GLint *${arg.name} = reinterpret_cast<GLint *>(_${arg.name}->GetPointer());
			if (!CheckParamCount(pname, _${arg.name}->GetSize())) {
				sig.SetError(ERR_ValueError,
						"the list must have %d elements", GetParamCount(pname));
				return Value::Null;
			}''',
			nil,
			nil)
	'GLsizei@' => TypeConv(
			'number', 'Once', ['List'],
			R'''
			AutoPtr<Array<GLsizei> > _${arg.name}(CreateArrayFromList<GLsizei>(args.GetList(${idx})));
			GLsizei *${arg.name} = _${arg.name}->GetPointer();
			''',
			nil,
			nil)
	'GLsizei@:const' => TypeConv(
			'number', 'Once', ['List'],
			R'''
			CArray<GLsizei> ${arg.name} = args.GetList(${idx});
			''',
			nil,
			nil)
	'GLuint@' => TypeConv(
			'number', 'Once', ['List'],
			R'''
			CArray<GLuint> ${arg.name} = args.GetList(${idx});
			''',
			nil,
			nil)
	'GLuint@:const' => TypeConv(
			'number', 'Once', ['List'],
			R'''
			CArray<GLuint> ${arg.name} = args.GetList(${idx});
			''',
			nil,
			nil)
	'GLuint@3:const' => TypeConv(
			'number', 'Once', ['List'],
			R'''
			CArray<GLuint> ${arg.name} = args.GetList(${idx});
			if (${arg.name}.GetSize() != 3) {
				sig.SetError(ERR_ValueError, "the list must have three elements");
				return Value::Null;
			}''',
			nil,
			nil)
	'GLuint@4:const' => TypeConv(
			'number', 'Once', ['List'],
			R'''
			CArray<GLuint> ${arg.name} = args.GetList(${idx});
			if (${arg.name}.GetSize() != 4) {
				sig.SetError(ERR_ValueError, "the list must have three elements");
				return Value::Null;
			}''',
			nil,
			nil)
	'GLuint@pname:const' => TypeConv(
			'number', 'Once', ['List'],
			R'''
			CArray<GLuint> ${arg.name} = args.GetList(${idx});
			if (!CheckParamCount(pname, ${arg.name}.GetSize())) {
				sig.SetError(ERR_ValueError,
						"the list must have %d elements", GetParamCount(pname));
				return Value::Null;
			}''',
			nil,
			nil)
	'GLfloat@' => TypeConv(
			'number', 'Once', ['List'],
			R'''
			CArray<GLfloat> ${arg.name} = args.GetList(${idx});
			''',
			nil,
			nil)
	'GLfloat@:const' => TypeConv(
			'number', 'Once', ['List'],
			R'''
			CArray<GLfloat> ${arg.name} = args.GetList(${idx});
			''',
			nil,
			nil)
	'GLfloat@1:const' => TypeConv(
			'number', 'Once', ['List'],
			R'''
			CArray<GLfloat> ${arg.name} = args.GetList(${idx});
			if (${arg.name}.GetSize() != 1) {
				sig.SetError(ERR_ValueError, "the list must have one element");
				return Value::Null;
			}''',
			nil,
			nil)
	'GLfloat@2:const' => TypeConv(
			'number', 'Once', ['List'],
			R'''
			CArray<GLfloat> ${arg.name} = args.GetList(${idx});
			if (${arg.name}.GetSize() != 2) {
				sig.SetError(ERR_ValueError, "the list must have two elements");
				return Value::Null;
			}''',
			nil,
			nil)
	'GLfloat@3:const' => TypeConv(
			'number', 'Once', ['List'],
			R'''
			CArray<GLfloat> ${arg.name} = args.GetList(${idx});
			if (${arg.name}.GetSize() != 3) {
				sig.SetError(ERR_ValueError, "the list must have three elements");
				return Value::Null;
			}''',
			nil,
			nil)
	'GLfloat@4:const' => TypeConv(
			'number', 'Once', ['List'],
			R'''
			CArray<GLfloat> ${arg.name} = args.GetList(${idx});
			if (${arg.name}.GetSize() != 4) {
				sig.SetError(ERR_ValueError, "the list must have four elements");
				return Value::Null;
			}''',
			nil,
			nil)
	'GLfloat@matrix:const' => TypeConv(
			'any', 'Once', [],
			R'''
			CArray<GLfloat> ${arg.name} = MakeMatrix<GLfloat>(sig, args.GetValue(${idx}));
			if (sig.IsSignalled()) return Value::Null;''',
			nil,
			nil)
	'GLfloat@pname:const' => TypeConv(
			'number', 'Once', ['List'],
			R'''
			CArray<GLfloat> ${arg.name} = args.GetList(${idx});
			if (!CheckParamCount(pname, ${arg.name}.GetSize())) {
				sig.SetError(ERR_ValueError,
						"the list must have %d elements", GetParamCount(pname));
				return Value::Null;
			}''',
			nil,
			nil)
	'GLclampf@:const' => TypeConv(
			'number', 'Once', ['List'],
			R'''
			CArray<GLclampf> ${arg.name} = args.GetList(${idx});
			''',
			nil,
			nil)
	'GLdouble@' => TypeConv(
			'number', 'Once', ['List'],
			R'''
			CArray<GLdouble> ${arg.name} = args.GetList(${idx});
			''',
			nil,
			nil)
	'GLdouble@:const' => TypeConv(
			'number', 'Once', ['List'],
			R'''
			CArray<GLdouble> ${arg.name} = args.GetList(${idx});
			''',
			nil,
			nil)
	'GLdouble@1:const' => TypeConv(
			'number', 'Once', ['List'],
			R'''
			CArray<GLdouble> ${arg.name} = args.GetList(${idx});
			if (${arg.name}.GetSize() != 1) {
				sig.SetError(ERR_ValueError, "the list must have one element");
				return Value::Null;
			}''',
			nil,
			nil)
	'GLdouble@2:const' => TypeConv(
			'number', 'Once', ['List'],
			R'''
			CArray<GLdouble> ${arg.name} = args.GetList(${idx});
			if (${arg.name}.GetSize() != 2) {
				sig.SetError(ERR_ValueError, "the list must have two elements");
				return Value::Null;
			}''',
			nil,
			nil)
	'GLdouble@3:const' => TypeConv(
			'number', 'Once', ['List'],
			R'''
			CArray<GLdouble> ${arg.name} = args.GetList(${idx});
			if (${arg.name}.GetSize() != 3) {
				sig.SetError(ERR_ValueError, "the list must have three elements");
				return Value::Null;
			}''',
			nil,
			nil)
	'GLdouble@4:const' => TypeConv(
			'number', 'Once', ['List'],
			R'''
			CArray<GLdouble> ${arg.name} = args.GetList(${idx});
			if (${arg.name}.GetSize() != 4) {
				sig.SetError(ERR_ValueError, "the list must have four elements");
				return Value::Null;
			}''',
			nil,
			nil)
	'GLdouble@4' => TypeConv(
			'number', 'Once', ['List'],
			R'''
			CArray<GLdouble> ${arg.name} = args.GetList(${idx});
			if (${arg.name}.GetSize() != 4) {
				sig.SetError(ERR_ValueError, "the list must have four elements");
				return Value::Null;
			}''',
			nil,
			nil)
	'GLdouble@matrix:const' => TypeConv(
			'any', 'Once', [],
			R'''
			CArray<GLdouble> ${arg.name} = MakeMatrix<GLdouble>(sig, args.GetValue(${idx}));
			if (sig.IsSignalled()) return Value::Null;''',
			nil,
			nil)
	'GLdouble@pname:const' => TypeConv(
			'number', 'Once', ['List'],
			R'''
			CArray<GLdouble> ${arg.name} = args.GetList(${idx});
			if (!CheckParamCount(pname, ${arg.name}.GetSize())) {
				sig.SetError(ERR_ValueError,
						"the list must have %d elements", GetParamCount(pname));
				return Value::Null;
			}''',
			nil,
			nil)
}

Implement = cbridge.Implement
AddTypeConv = cbridge.AddTypeConv
AddTypeConvs = cbridge.AddTypeConvs
Renderer = cbridge.Renderer
