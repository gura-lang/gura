import(zip)

!path.exists('tmp') && fs.mkdir('tmp')
//println("'", path.walk('test.zip/')::replace('\\', '\\\\'), "'")
//sys.exit()
files = path.walk('test.zip/')

for (file in files) {
	println(file)
	copy(file, open(path.join('tmp', path.filename(file)), 'w'))
}
