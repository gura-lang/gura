#!/usr/bin/env gura
import(console)
colors = [
	`black, `blue, `green, `cyan, `red, `magenta, `yellow, `white
	`gray, `bright_blue, `bright_green, `bright_cyan,
	`bright_red, `bright_magenta, `bright_yellow, `bright_white
]
console.clear()
for (fg in colors) {
	for (bg in colors) {
		console.setcolor(fg, bg) {
			print('####')
		}
	}
	println()
}
console.moveto(3..20, 1..) {
	print('hello')
}
