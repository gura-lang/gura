#!/usr/bin/env gura
import(helper.test) {*}
import(ml.mnist)

testcase('ImageSet') {
	imageSet = ml.mnist.ImageSet('sample/t10k-images-idx3-ubyte')
	println(imageSet)
	imageSet.ToArray(`flat) {|arr|
		println(arr[0])
	}
}
testcase('LabelSet') {
	labelSet = ml.mnist.LabelSet('sample/t10k-labels-idx1-ubyte')
	println(labelSet)
	labelSet.ToArray {|lbl|
	//	println(lbl)
	}
	
}
