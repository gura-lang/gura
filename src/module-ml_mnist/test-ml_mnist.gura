#!/usr/bin/env gura
import(helper.test) {*}
import(ml.mnist)
import(gzip)

db = ml.mnist.Database('sample')
printf('Test .. nImages:%d, nRows:%d, nColumns:%d, nLabels:%d\n',
	   db.test.imageSet.nImages, db.test.imageSet.nRows, db.test.imageSet.nColumns,
	   db.test.labelSet.nLabels)
printf('Train .. nImages:%d, nRows:%d, nColumns:%d, nLabels:%d\n',
	   db.train.imageSet.nImages, db.train.imageSet.nRows, db.train.imageSet.nColumns,
	   db.train.labelSet.nLabels)
db.train.imageSet.ToArray(`flat)
db.train.labelSet.ToArray()
