#!/usr/bin/env gura
import(helper.test) {*}
import(ml.mnist)

testcase('ImageSet') {
	imageSet = ml.mnist.ImageSet('sample/t10k-images-idx3-ubyte')
	println(imageSet)
}
testcase('LabelSet') {
	imageSet = ml.mnist.LabelSet('sample/t10k-labels-idx1-ubyte')
	println(imageSet)
}
