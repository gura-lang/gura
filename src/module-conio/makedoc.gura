#!/usr/bin/env gura
import(markdown)
import(conio)

write(doc:markdown.document) = {
	doc << [
		conio.clear
		conio.getwinsize
		conio.setcolor
		conio.moveto
		conio.waitkey
	]
	doc
}

if (__name__ == '__main__') {
	write(markdown.document()).render@html(sys.stdout)
}
