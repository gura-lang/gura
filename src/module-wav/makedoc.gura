#!/usr/bin/env gura
import(markdown)
import(wav)

text = R'''
# wav Module
## Abstract
## Sample
'''

write(doc:markdown.document) = {
	doc << text
	doc << '## Module Functions'
	doc << [
	]
	doc << '## Extension of audio Class'
	doc << [
		audio.read@wav
		audio.write@wav
	]
	doc
}

if (__name__ == '__main__') {
	write(markdown.document()).render@html(sys.stdout)
}
