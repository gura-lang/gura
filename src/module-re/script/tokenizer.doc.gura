#!/usr/bin/env gura
import(markdown)

text@en = R'''
# tokenizer Module

The `tokenizer` module ...

## Module Function

${markdown.makedoc@function(`en
)}

'''.embed()

markdown.makedoc@module(locals(), %{`en => text@en})
