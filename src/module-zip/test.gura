#!/usr/bin/env gura
import(zip)

!path.exists('tmp') && fs.mkdir('tmp')
//println("'", path.walk('test.zip/')::replace('\\', '\\\\'), "'")
//sys.exit()
path.walk('test.zip/') {|fileName|
	println(fileName)
	copy(fileName, open(path.join('tmp', path.filename(fileName)), 'w'))
}
