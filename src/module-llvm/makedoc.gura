#!/usr/bin/env gura
import(markdown)

text = R'''
# llvm Module

## Module Functions
'''.template().render()

write(doc:markdown.document) = {
	doc << text
	doc
}

if (__name__ == '__main__') {
	write(markdown.document()).render@html(sys.stdout)
}
