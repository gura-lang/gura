#!/usr/bin/env gura
import(helper.test) {*}
import(model.stl)

testcase {
	//reader1 = model.stl.reader('cube-ascii.stl')
	//reader2 = model.stl.reader('cube-binary.stl')
	//println(reader1)
	//println(reader1, '\n', reader2)
	reader = model.stl.reader('Voyager_17.stl')
	//println(reader.solidname)
	println(reader)
}
