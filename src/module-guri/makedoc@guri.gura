#!/usr/bin/env gura
import(markdown)

text = R'''
# guri Module

### Module Function

'''

write(doc:markdown.document) = {
	doc << text
	doc
}

if (__name__ == '__main__') {
	write(markdown.document()).render@html(sys.stdout)
}
