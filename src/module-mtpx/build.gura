#!/usr/bin/env gura
import(modbuild)

builder = modbuild.Builder()
builder.libs.append('ShlWapi.lib', 'PortableDeviceGuids.lib')
builder.build('mtp', [
	'module-mtp.cpp', 'Platform_msw.cpp',
	'Class_device.cpp', 'Class_storage.cpp', 'Class_stat.cpp'])
