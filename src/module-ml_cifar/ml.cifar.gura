#!/usr/bin/env gura
import(tar)
import(ml.cifar):binary:mixin_type {*}

if (__name__ == '__main__') {
	//'sample/cifar-10-binary.tar.gz/cifar-10-batches-bin/data_bach_1.bin'
	reader = tar.reader('sample/cifar-10-binary.tar.gz')
	println(reader.entries():*stat:*filename)
}
