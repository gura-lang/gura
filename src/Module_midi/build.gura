#!/usr/bin/env gura
import(modbuild)

builder = modbuild.Builder()
if (sys.build == `msc) {
	// nothing to do
} elsif (sys.build == `gnuc) {
	builder.libs.append('asound')
}
builder.build('midi', ['Module_midi.cpp',
		'Port.cpp', 'Event.cpp', 'Track.cpp', 'MML.cpp', 'SMF.cpp'])
