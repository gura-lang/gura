#!/usr/bin/env gura
import(mswin)
import(freetype)

pathnames = path.dir(freetype.sysfontpath(), '*.ttc', '*.ttf'):icase
filenames = path.filename(pathnames)
ctxs = freetype.Context(pathnames)
printf('%-*s %s\n', filenames:*len().max(), filenames, ctxs:*family_name)
