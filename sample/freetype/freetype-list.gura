#!/usr/bin/env gura
import(mswin)
import(freetype)

pathnames = path.dir(freetype.sysfontpath(), '*.ttc', '*.ttf'):icase
filenames = path.filename(pathnames)
fonts = freetype.font(pathnames)
printf('%-*s %s\n', filenames:*len().max(), filenames, fonts:*family_name)
