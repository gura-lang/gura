#!/usr/bin/env gura
import(plotter)

plotter.canvas(`horz, 2) {|cv|
	xs = [10, -10, -10, 10]
	ys = [10, 10, -10, -10]
	cv.graph {|g|
		g.title = 'No adjustment'
		g.plot(xs, ys)
	}
	cv.graph {|g|
		g.title = 'adjustRange()'
		g.plot(xs, ys)
		g.adjustRange()
	}
	cv.graph {|g|
		g.title = 'adjustRange(20)'
		g.plot(xs, ys)
		g.adjustRange(20)
	}
	cv.graph {|g|
		g.title = 'adjustRange(20, 30)'
		g.plot(xs, ys)
		g.adjustRange(20, 30)
	}
	cv.show()
}
